{
  "version": "v1",
  "hooks": [
    {
      "name": "Block writes to secret files",
      "description": "A PreToolUse gate. Refuses any tool write whose target is an environment or credential file, and refuses to write a value that looks like a Supabase JWT or a Bedrock key into tracked source. Secrets leaking into a repository is the one mistake that cannot be undone by a later commit.",
      "trigger": "PreToolUse",
      "matcher": "fs_write|execute_bash",
      "action": {
        "type": "command",
        "command": "./scripts/hooks/guard-secrets.sh"
      },
      "timeout": 20
    }
  ]
}
